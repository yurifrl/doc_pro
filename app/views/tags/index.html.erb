<h1>Listagem de Tags</h1>

<table>
  <thead>
    <tr>
      <th>Nome</th>
      <th>Tag</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @tags.each do |tag| %>
      <tr>
        <td><%= tag.name %></td>
        <td><%= tag.tag_name %></td>
        <td><%= link_to 'Show', tag %></td>
      </tr>
    <% end %>
  </tbody>
</table>