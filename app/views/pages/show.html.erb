<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @page.name %>
</p>

<p>
  <strong>Tag name:</strong>
  <%= @page.system_name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @page.description%>
</p>

<p>
  <strong>Possui os objetos:</strong>
  <% @page.tags.map do |tag| %>
      <%= link_to tag.tag_name, tag_path(tag)%> ,
  <% end %>
</p>

<%= link_to 'Back', pages_path %>
