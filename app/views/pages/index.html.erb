<h1>Listagem de Paginas</h1>
<table>

  <% Content.all.each do |content| %>
      <thead>
        <tr>
          <td><h5><%= content.name %></h5></td>
        </tr>
      </thead>
      <tbody>
        <% content.pages.each do |page| %>
            <tr>
              <td><%= link_to page.system_name, page %></td>
            </tr>
        <% end %>
      </tbody>
  <% end %>
</table>